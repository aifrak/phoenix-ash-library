defmodule Library.Repo.Migrations.UseUuidV7 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:feedback_reviews) do
      modify :id, :uuid, default: fragment("uuid_generate_v7()")
    end

    alter table(:feedback_comments) do
      modify :id, :uuid, default: fragment("uuid_generate_v7()")
    end

    alter table(:feedback_authors) do
      modify :id, :uuid, default: fragment("uuid_generate_v7()")
    end

    alter table(:catalog_books_authors) do
      modify :id, :uuid, default: fragment("uuid_generate_v7()")
    end

    alter table(:catalog_books) do
      modify :id, :uuid, default: fragment("uuid_generate_v7()")
    end

    alter table(:catalog_authors) do
      modify :id, :uuid, default: fragment("uuid_generate_v7()")
    end
  end

  def down do
    alter table(:catalog_authors) do
      modify :id, :uuid, default: fragment("gen_random_uuid()")
    end

    alter table(:catalog_books) do
      modify :id, :uuid, default: fragment("gen_random_uuid()")
    end

    alter table(:catalog_books_authors) do
      modify :id, :uuid, default: fragment("gen_random_uuid()")
    end

    alter table(:feedback_authors) do
      modify :id, :uuid, default: fragment("gen_random_uuid()")
    end

    alter table(:feedback_comments) do
      modify :id, :uuid, default: fragment("gen_random_uuid()")
    end

    alter table(:feedback_reviews) do
      modify :id, :uuid, default: fragment("gen_random_uuid()")
    end
  end
end
