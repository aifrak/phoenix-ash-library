defmodule Library.Repo.Migrations.UseIdAsPrimaryKeyForFeedbackReview do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    drop constraint("feedback_reviews", "feedback_reviews_pkey")

    alter table(:feedback_reviews) do
      modify :author_id, :uuid
      modify :book_id, :uuid
    end

    execute("ALTER TABLE \"feedback_reviews\" ADD PRIMARY KEY (id)")
  end

  def down do
    alter table(:feedback_reviews) do
      modify :book_id, :uuid
      modify :author_id, :uuid
    end

    execute("ALTER TABLE \"feedback_reviews\" DROP constraint feedback_reviews_pkey")

    execute("ALTER TABLE \"feedback_reviews\" ADD PRIMARY KEY (id, book_id, author_id)")
  end
end
